pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Cross_Platform_SVG_Parser"
include(":androidApp")
include(":shared")